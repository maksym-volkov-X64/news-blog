{
  "name": "@news-blog/app",
  "private": true,
  "scripts": {
    "dev": "flutter run -d linux",
    "build:linux": "flutter build linux",
    "build:android": "flutter build apk",
    "build:ios": "flutter build ios",
    "build": "flutter build linux && flutter build apk",
    "test": "flutter test",
    "lint": "flutter analyze"
  }
}
