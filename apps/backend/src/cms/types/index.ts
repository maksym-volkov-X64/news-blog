export enum UserRole {
  Root = 'root',
  Admin = 'admin',
  ContentManager = 'content-manager',
  Customer = 'customer',
}

export enum Collection {
  Posts = 'posts',
  Media = 'media',
  Users = 'users',
}
